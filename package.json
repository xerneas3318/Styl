{
  "name": "styl",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "build:chrome": "node build.js --chrome",
    "watch": "node build.js --watch",
    "watch:chrome": "node build.js --watch --chrome",
    "package:firefox": "node build.js --package",
    "package:chrome": "node build.js --package --chrome",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/firefox-webext-browser": "^120.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.4.0"
  }
}
